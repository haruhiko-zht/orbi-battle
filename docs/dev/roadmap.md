# 開発ロードマップ

基準日: **2025-10-18**

確定した短期タスクのみを掲載します。構想レベルのアイデアは Issue で管理してください。

## 注力中（〜10月下旬）

- **回避行動のプロトタイプ** — 攻撃射程サークルを利用して近接戦術に簡易回避ロジックを追加。決定論を崩さずに導入する方法を検証。
- **HP UI 微調整** — 3v3 以上で HP バーが重ならないよう余白とフォントサイズを調整。HUD テストの更新もセットで行う。

## 次の候補（着手順未定）

- **カバレッジ監視** — シミュレーションテストのスナップショット依存を解消し、明示的アサーションへ移行。
- **イベントフック整備** — `BattleRuntimeController` でヒット／死亡イベントを公開し、演出とログ解析を強化。
- **プリセット整理** — `defaults3v3` / `mixedTactics3v3` の開始位置を `arenaRadius` と連動させ、初期重なりを解消。

## 最近完了

- **戦術選択 UI** — デバッグパネルから `nearest` / `aggressive` / `defensive` を即時切り替えできるドロップダウンを実装。
- `window.$orbi` の型定義を `src/types/global.d.ts` に追加し、`@ts-expect-error` を排除。
- 攻撃射程サークルを描画し、デフォルトプリセットの射程を 9〜10px に再調整。
- `simulateBattle` 入口で `validation.ts` による入力チェックを共通化。

## 運用メモ

- orbi-battle は 2 チーム構成が前提。チーム追加は描画・UI の仕様改修が必要。
- 完了タスクは `docs/changelog.md` へ移し、ここから削除する。
